{% extends "base.html.twig" %}

{% block body %}
    {%  if error is defined %}
        <h1 style="color:red">{{ error }}</h1>
    {% endif %}
    {{ form(loginForm, {"action": path('loginHandle')}) }}

{% endblock %}

